spring.application.name=config-server
#conversion port
server.port=${CONFIG-SERVER-PORT}

#connect to eureka server for registration
eureka.client.service-url.defaultZone=${EUREKA-CLIENT-URL}

#External centralization(GITHUB)
spring.cloud.config.server.git.uri =${GITHUB_URI}
spring.cloud.config.server.git.clone-on-start= true
spring.cloud.config.server.git.username=karangwaajika
spring.cloud.config.server.git.password=${GITHUB-TOKEN}

